############| +Custom Icons Addon |###########################>
custom_icons:
  variables:
    _icon: "mdi:wifi"
    _icon_on: "mdi:wifi-star"
    _icon_unavailable: "mdi:wifi-cancel"
  icon: >-
    [[[
      let i = variables._icon
      switch(entity.state) {
        case "on":
          i = variables._icon_on
          break
        case "unavailable":
          i = variables._icon_unavailable
          break
      }

      return i
    ]]]
  # state:
  #   - value: "on"
  #     icon: "[[[ return variables._icon_on ]]]"
  #   - value: "unavailable"
  #     icon: "[[[ return variables._icon_unavailable ]]]"